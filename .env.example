# Copy to .env.local for dev or set in CI for production builds.

# Local (Firebase emulator — run: pnpm emulator:functions)
# VITE_ENQUIRY_SUBMIT_URL=http://127.0.0.1:5001/gridandgoal-e7880/us-central1/submitEnquiry

# Production (after deploy)
# VITE_ENQUIRY_SUBMIT_URL=https://asia-south1-YOUR_PROJECT_ID.cloudfunctions.net/submitEnquiry
